Question 1) 1) Develop a Menu driven program to demonstrate the following operations of Arrays
——MENU——-
1.CREATE
2.DISPLAY
3.INSERT
4.DELETE
5.LINEAR SEARCH
6.EXIT

Answer 1) 
#include<stdio.h>
int arr[100];
n = 0;
void create() 
{
    printf("Enter number of elements: ");
    scanf("%d", &n);
    printf("Enter %d elements:\n", n);
    for (int i = 0; i < n; i++)
        scanf("%d", &arr[i]);
}
void display() 
{
    printf("Array: ");
    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
void insert() {
    int position, value;
    printf("Enter position: ");
    scanf("%d", &position);
    printf("Enter value: ");
    scanf("%d", &value);
    for (int i = n; i >= position; i--)
        arr[i] = arr[i - 1];
    arr[position - 1] = value;
    n++;
}
void delete()
{
    int position;
    printf("Enter position: ");
    scanf("%d", &position);
    for (int i= position-1; i<n-1; i++)
        arr[i] =arr[i+1];
    n--;
}
void search() 
{
    int value, found = 0;
    printf("Enter value to search: ");
    scanf("%d", &value);
    for(int i=0; i<n; i++)
        if (arr[i] == value) {
            printf("Found at position %d\n", i + 1);
            found = 1;
        }
        if (!found)
            printf("Not found\n");
}
int main() {
    int ch;
    while(1)
{
        printf("\n1.CREATE\n2.DISPLAY\n3.INSERT\n4.DELETE\n5.SEARCH\n6.EXIT\n");
        printf("Enter choice: ");
        scanf("%d", &ch);
        switch(ch){
            case 1: create(); break;
            case 2: display(); break;
            case 3: insert(); break;
            case 4: delete(); break;
            case 5: search(); break;
            case 6: return 0;
            default: printf("Invalid choice\n");
        }
    }
}
