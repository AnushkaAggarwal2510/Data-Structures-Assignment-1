Question 4(a) Implement the logic to reverse the elements of an array.

Answer 4(a)
#include <stdio.h>
int main()
{
    int arr[100], n, first, last, temp;
    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter %d elements:\n",n);
    for(int i=0;i<n;i++)
        scanf("%d",&arr[i]);

    first=0;
    last= n-1;
    while(first<last) {
        temp= arr[first];
        arr[first]= arr[last];
        arr[last]= temp;
        first++;
        last--;
    }
    printf("Reversed array:\n");
    for (int i=0;i<n;i++)
        printf("%d ",arr[i]);
    return 0;
}
